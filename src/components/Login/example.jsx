import { useState } from "react"

const Example = () => {

    const [selectedLight, setSelectedLight] = useState("")

    const hola = "";

  setSelectedLight("verde fluorrr");

    return(
        <div>
            <div className="red" onClick={() => {}}></div>
        </div>
    )
}